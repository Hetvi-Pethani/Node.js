<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View User</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        h2 {
            text-align: center;
            color: #3b6a73;
            margin-bottom: 5%;
        }

        .m-img {
            border-radius: 15px;
            width: 50%;

        }

        a {
            text-decoration: none;
            color: #fff;
            background-color: #2c6e7b;
            padding: 15px;
            margin: 20px 0;
            border-radius: 10px;
            display: inline-block;

        }
        h5{
            color: #2c6e7b;
            font-size: 20px;
        }
        p{
            color: #2c6e7b;
            font-size: 18px;
        }
    </style>
</head>

<body>

    <div class="container mt-5">
        <div class="text-center mb-4">
            <h2>Your Tickets Book</h2>
        </div>
        <div class="container ">
          <div class="row col-6">
            <% record.map((val, i)=> { %>
                <div class="m-img">
                    <img src="<%= val.image %>" width="300px" alt="">
                    <div class="m-add text-center">
                        <h5>
                            <%= val.name %>
                        </h5>

                        <p>Movie Price:- <%= val.phone %>₹</p>
                        <div>
                            <a href="/deleteuser?did=<%= val?.id %>" >Delete</a>
                            <a href="/updateuser?id=<%= val?.id %>">Edit</a>
                        </div>

                    </div>
                </div>
                <% }) %>
          </div>
        </div>
       
            <a href="form">Add New User</a>
      

    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>